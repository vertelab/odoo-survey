<?xml version="1.0"?>
<odoo>
    <record model="ir.ui.view" id="view_survey_mail_form">
        <field name="name">survey.mail.form</field>
        <field name="model">survey.mail</field>
        <field name="arch" type="xml">
            <form string="Survey Mail Scheduler">
                <sheet>
                    <group>
                        <group>
                            <field name="survey_id" />
                            <field name="notification_type" />
                            <field name="template_ref" options="{'hide_model': True, 'no_quick_create': True}" context="{'filter_template_on_event': True, 'default_model': 'survey.registration'}" />
                            <field name="mail_state" />
                        </group>
                        <group>
                            <label for="interval_nbr" />
                            <div class="o_row">
                                <field name="interval_nbr" invisible="interval_unit == 'now'" readonly="interval_unit == 'now'" />
                                <field name="interval_unit" />
                            </div>
                            <field name="interval_type" />
                            <field name="scheduled_date" />
                        </group>
                    </group>
                    <notebook groups="base.group_no_one">
                        <page string="Registration Mails" name="registration_mails">
                            <field name="mail_registration_ids">
                                <list string="Registration mail" editable="bottom">
                                    <field name="registration_id" />
                                    <field name="scheduled_date" />
                                    <field name="mail_sent" string="Sent" />
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record model="ir.ui.view" id="view_survey_mail_tree">
        <field name="name">survey.mail.tree</field>
        <field name="model">survey.mail</field>
        <field name="arch" type="xml">
            <list string="Survey Mail Schedulers">
                <field name="survey_id" />
                <field name="notification_type" />
                <field name="template_ref" options="{'hide_model': True, 'no_quick_create': True}" context="{'filter_template_on_event': True, 'default_model': 'survey.registration'}" />
                <field name="scheduled_date" />
                <field name="mail_count_done" />
                <field name="mail_state" widget="survey_icon_selection" string=" " nolabel="1" options="{'sent': 'fa fa-check', 'scheduled': 'fa fa-hourglass-half', 'running': 'fa fa-cogs'}" />
            </list>
        </field>
    </record>

    <record model="ir.actions.act_window" id="action_survey_mail">
        <field name="name">Surveys Mail Schedulers</field>
        <field name="res_model">survey.mail</field>
        <field name="context">{'create': False}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">Nothing Scheduled yet!</p>
            <p>Under this technical menu you will find all scheduled communication related to your surveys.</p>
        </field>
    </record>

</odoo>


        <!--
               <record id="menu_survey_mail_schedulers" model="ir.ui.menu" name="Survey Mail Scheduler">
          <record id="menu_survey_mail_schedulers" model="ir.ui.menu">
            <field name="action" ref="survey_mail.action_survey_mail" />
          </record>
        -->
